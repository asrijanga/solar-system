[project]
name = "solar-system-pipeline"
version = "0.0.0"
description = "Scripted, checksummed conversion of planetary and stellar data into app assets"
requires-python = ">=3.11,<3.12"
dependencies = []
